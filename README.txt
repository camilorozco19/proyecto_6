 DSS - Versión completa con Leaflet, login en la UI y JWT para la API.

Pasos rápidos para ejecutar:
1. Instalar Python 3.10+
2. Instalar dependencias:
   pip install -r requirements.txt
3. Ejecutar:
   python app.py
4. Abrir en el navegador: http://127.0.0.1:5000

Usuarios demo (navegación):
- admin / admin123  (rol: admin) -> puede subir archivos
- analyst / analyst123 (rol: analyst) -> puede ver análisis

Nota: Para que el mapa muestre puntos, tu archivo debe tener columnas: latitude, longitude.